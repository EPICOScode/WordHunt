 import Home from "../..";

const Vowel = () => {

    let vowels = "";

    for (i=0; i < something.length; i++ ){
        let vowel = something[i]; 
        if (/[aeiou]/.test.vowel)
            console.log(vowels)
    }

    return (
        <>
          
        </>
    )
}; 

export default Vowel;